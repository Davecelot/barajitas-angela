# Spec-Driven Development

Use `.sdd/active/` for specs that are being planned or implemented.
Use `.sdd/archive/` for completed specs after validation and review.

Each active spec should include:

- Why
- What
- Constraints
- Current State
- Tasks with verify steps
- Validation

Archive a spec only after its validation commands pass and the implementation no
longer needs active SDD tracking.
